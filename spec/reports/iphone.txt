
Capybara::Session
  with non-existant driver
    should raise an error

Capybara::Session
  with terminus driver
    it should behave like session
      should encode complex field names, like array[][value] (FAILED - 1)
      #visit
        should fetch a response from the driver (FAILED - 2)
      #body
        should return the unmodified page body (FAILED - 3)
      #html
